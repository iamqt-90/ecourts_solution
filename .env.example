# Environment variables for eCourts Scraper

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# API settings (for optional web interface)
API_DEBUG=False
API_HOST=0.0.0.0
API_PORT=5000

# Request settings
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# Output settings
DEFAULT_OUTPUT_FORMAT=json
OUTPUT_DIRECTORY=output